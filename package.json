{
  "name": "qmh-damage-register",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Root package.json — exists so the Node function at api/blob-upload-url.js can resolve @vercel/blob. The actual frontend app lives in frontend/ with its own package.json.",
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  }
}
