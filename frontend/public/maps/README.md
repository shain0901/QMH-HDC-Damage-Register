Drop the marked-up building photos in here, named to match each building:

    chisholm-block.jpg
    nurses-hostel.jpg
    soldiers-block.jpg

(.png/.webp also fine — see the `MAP_IMAGE_EXT` map in `frontend/src/components/BuildingMapCard.jsx`
if you use a different extension per file.) Whatever you drop in here shows up in the Building Map
card automatically on the next deploy — no code changes needed.
